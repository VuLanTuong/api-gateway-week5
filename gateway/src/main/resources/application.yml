spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://localhost:3000"
            allowedHeaders: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
      routes:
        - id: order
          uri: http://localhost:8801
          predicates:
            - Path=/api/v2/**
        - id: CustomerInfo
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/**
      